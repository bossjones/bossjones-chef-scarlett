load '../../config/Berksfile.config'

load_source

metadata

cookbook "users", path: "../users"
cookbook "sudo", path: "../sudo"

#group :integration do
#  behance_cookbook 'bebootstrap', '~> 3.1'
#end
